/**
 * @deprecated Import depuis `@/src/projects/shared/branding/AppLocaleProvider` :
 * `useAppLocale`, `AppLocaleProvider`, `readStoredAppLocale`, `applyDocumentLocale`, etc.
 */
export {
  AppLocaleProvider,
  applyDocumentLocale,
  APP_LOCALE_COOKIE,
  APP_LOCALE_STORAGE_KEY,
  readStoredAppLocale,
  useAppLocale,
} from "./AppLocaleProvider";
